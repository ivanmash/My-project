<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <EditText android:id="@+id/user_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cursorVisible="true"
        android:hint="Введите Ваш номер телефона"
        android:layout_centerInParent="true"
        android:singleLine="true" />
    <EditText android:id="@+id/Arduino_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cursorVisible="true"
        android:hint="Введите номер телефона модуля"
        android:layout_centerInParent="true"
        android:layout_above="@+id/user_number"
        android:singleLine="true" />

    <Button android:id="@+id/OK"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_below="@+id/user_number"
        android:gravity="center"
        android:text="Отправить"
        android:onClick="onClick" />
</RelativeLayout>
